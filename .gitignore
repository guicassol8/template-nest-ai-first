node_modules/
dist/
coverage/
*.tsbuildinfo

.env
.env.*
!.env.example

*.log
.DS_Store
.idea/
.vscode/

# Override local do compose (ex.: remapear a porta do Postgres se a 5432 já
# estiver ocupada por outro projeto). Nunca commitado.
docker-compose.override.yml
dist-openapi/

# Client do Prisma: gerado por `pnpm prisma generate`, nunca commitado.
src/generated/
